export const api_url = process.env.NODE_ENV === 'production' ? '/router' : '/api'
export const file_url = process.env.NODE_ENV === 'production' ? '/router' : '/fileUrl'
export const PAGE_SIZE = 20
export const regionCd = '42'
